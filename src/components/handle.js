import { View } from "react-native";
import ChooseDate from "/calendar"

//This should handle data from calendar, distance, time and skill. 
export default function Handle() {
    return(
        <View>
            
        </View>
    );
}